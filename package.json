{
  "name": "stories_schema_library",
  "version": "0.0.2",
  "description": "Stories schema library",
  "author": {
    "name": "KeeeX SAS",
    "email": "contact@keeex.me",
    "url": "https://keeex.me"
  },
  "scripts": {
    "deref": "node ./deref -i jsonSchema -o compiled",
    "build-test": "node ./build -c \"TEST BLB\" -i compiled -s hash_test_blb.json -o schema_idx_test_blb.json",
    "build": "node ./build -c \"BLB\" -i compiled -s hash_test_blb.json -o schema_idx_test_blb.json"
  },
  "dependencies": {
    "json-schema-ref-parser": "^7.1.2",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7",
    "yargs": "^14.0.0"
  }
}
